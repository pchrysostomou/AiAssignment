# Project Summary
The Academic Suite is an advanced SaaS platform aimed at enhancing academic writing and integrity. It offers a comprehensive suite of tools for essay generation, plagiarism checking, AI detection, and strict grading. The platform leverages sophisticated AI models to produce high-quality essays while ensuring compliance with citation standards. Users can upload documents and manage payments through Stripe, with flexible pricing based on word count and tool selection.

# Project Module Description
The application consists of several key modules:
- **User Management**: Manages user registration and authentication using Flask-Login.
- **Essay Generation (The Architect)**: Generates essays with citation verification and human-like writing styles using AI models.
- **Plagiarism Checking (The Detective)**: Conducts thorough checks for potential plagiarism and generates detailed PDF reports.
- **AI Detection (The Oracle)**: Identifies AI-generated content and provides a probability score.
- **Strict Grading (The Grader)**: Evaluates essays against assignment briefs using multiple AI models for comprehensive feedback.
- **File Upload & Parsing**: Supports drag-and-drop uploads of `.pdf`, `.docx`, and `.txt` files.
- **Stripe Payments**: Manages payments based on tool usage with tiered pricing.
- **Real-time Streaming**: Offers live updates on essay generation and tool processing.
- **AI Support Bot (The Concierge)**: Provides assistance via a chat interface for user inquiries.

# Directory Tree
```
ai-assignment-architect/
├── app.py                 # Main Flask application with backend logic for all tools
├── requirements.txt       # Python dependencies including Stripe and PDF generation libraries
├── .env.template          # Environment variables template for configuration
├── .env                   # Actual environment configuration (not in git)
├── ai_architect.db        # SQLite database (auto-created)
├── templates/
│   ├── base.html          # Master layout with navbar and support chat widget
│   ├── dashboard.html     # Control center for tool access and management
│   ├── tool_plagiarism.html # Frontend for the plagiarism detection tool
│   └── ...                # Other tool pages
├── README.md              # Documentation for installation and usage
└── start.bat             # Windows quick-start script
```

# File Description Inventory
- **app.py**: Core application logic, including routes for user management, tool operations, and payment processing.
- **requirements.txt**: Lists necessary Python libraries for the application, including Stripe, reportlab, and file handling libraries.
- **.env.template**: Template for environment variables needed to configure the application.
- **.env**: Contains sensitive configuration details (API keys, database URI).
- **ai_architect.db**: SQLite database file created at runtime to store user and essay data.
- **templates/base.html**: Common layout for all pages, including a persistent navbar and support chat.
- **templates/dashboard.html**: Main user interface to access tools and view history.
- **templates/tool_plagiarism.html**: User interface for the plagiarism detection tool.
- **README.md**: Comprehensive documentation for installation, usage, and features.
- **start.bat**: Batch file to automate the setup process on Windows.

# Technology Stack
- **Backend**: Flask (Python), Flask-SQLAlchemy (SQLite DB), Flask-Login for authentication.
- **Frontend**: HTML/JavaScript with Server-Sent Events (SSE) for real-time updates.
- **File Handling**: PyPDF2 for PDF parsing, python-docx for Word document processing.
- **Payments**: Stripe for payment processing.
- **AI Models**: Claude 3.5 Sonnet, GPT-4o, Gemini 1.5 Pro.

# Usage
### Installation Steps
1. **Clone or create the project directory**:
   ```bash
   mkdir ai-assignment-architect
   cd ai-assignment-architect
   ```

2. **Create a virtual environment**:
   ```bash
   python -m venv venv
   venv\Scripts\activate  # Windows
   ```

3. **Install dependencies**:
   ```bash
   pip install -r requirements.txt
   ```

4. **Configure environment variables**:
   ```bash
   # Copy the template
   copy .env.template .env
   # Edit .env and add your API keys
   notepad .env
   ```

5. **Initialize the database**:
   ```bash
   python app.py
   ```

### Running the Application
Start the application:
```bash
python app.py
```

### Using the Application
1. Login using the provided credentials.
2. Access the control center to select tools (The Architect, The Detective, The Oracle, The Grader).
3. Follow the prompts for each tool to upload documents or enter text and complete the associated tasks.
